# 第二章

## 2.8

生成一个 Possion 随机变量。令 $U_1,U_2,\cdots$ 是独立的 $(0,1)$ 均匀随机变量。

### (a). 如果 $X_i=(-\ln U_i)/\lambda$，证明 $X_i$ 是具有失效率 $\lambda$ 的指数随机变量

由于：
$$
P(X_i\le x)=P(\frac{-lnU_i}{\lambda}\le x)=P(-lnU_i\le {\lambda}x)=P({U_i}\ge e^{-\lambda x})
$$
因为 $U_i$ 是独立的 (0,1)均匀随机变量，有
$$
P(U_i\leqslant x)=x,\ P(U_i>x)=1-x
$$
所以有
$$
\begin{aligned}
P({U_i}\ge e^{-\lambda x})&=1-e^{-\lambda x}\\
P(X_i\le x)&=1-e^{-\lambda x}
\end{aligned}
$$

所以 $X_i$ 是具有失效率 $\lambda$ 的指数随机变量

### (b). 用 (a) 部分证明，当 $N$ 定义为等于满足 $$\prod_{i=1}^nU_i\geqslant e^{-\lambda}\geqslant \prod_{i=1}^{n+1}U_i$$ 的 $n$ 的值时，$N$ 是均值 $\lambda$ 的 Possion 随机变量，其中 $\prod_{i=1}^0U_i\neq1$

以 $X_i$ 记 Poisson 过程的到达间隔时间，则直到时刻 1 为止的事件数 $N(1)$ 等于满足
$$
\sum_{i=1}^{n}X_i<1<\sum_{i=1}^{n+1}X_i
$$
的 n，令 $X_i=-\ln U_i/\lambda$，上式等价于
$$
-\sum_{i=1}^{n}lnU_i<\lambda<-\sum_{i=1}^{n+1}lnU_i
$$
的 n，或等价于
$$
\sum_{i=1}^{n}lnU_i>-\lambda>\sum_{i=1}^{n+1}lnU_i
$$
等价于
$$
\ln\prod_{i=1}^n U_i>-\lambda>\ln\prod_{i=1}^{n+1}U_i
$$
等价于
$$ 
\prod_{i=1}^{n}U_i>e^{-\lambda}>\prod_{i=1}^{n+1}U_i
$$
结果得自 $N(1)$ 是均值为 $\lambda$ 的 Poisson 随机变量